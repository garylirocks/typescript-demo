import React from 'react';
import { gql } from '@apollo/client';

const a: string = 20;
const a: number = 20;

console.log(a);

const App = () => <div>TypeScript is running</div>;

export default App;
